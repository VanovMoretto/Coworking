import React from "react"
import './Button.css'

export default props => {
    return (
        <div className="buttons"> 
            <button className="horario">Horários</button>
            <button className="reserva">Reservar</button>
        </div>

    )
}